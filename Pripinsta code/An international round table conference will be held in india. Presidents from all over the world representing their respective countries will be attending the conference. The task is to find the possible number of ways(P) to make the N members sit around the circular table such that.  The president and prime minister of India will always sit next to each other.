#include <bits/stdc++.h>
using namespace std;
int fact(int n){
    int fact=1;
    for(int i=1;i<=n;i++){
        fact*=i;
    }
    return fact;
}
int main(){
    int n;
    cin>>n;
    int ways;
    ways=fact(2);
    int ways1;
    ways1=fact(n-1);
    int res=ways*ways1;
    cout<<res;
}
